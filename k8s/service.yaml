apiVersion: v1
kind: Service
metadata:
  name: fapi-qexec-svc
  labels:
    app: fapi-qexec
spec:
  selector:
    app: fapi-qexec
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
